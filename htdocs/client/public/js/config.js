/* ============================================================================
   Nom       : Allemand Jeremy
   Matricule : 20312390
   Cours     : IFT-1147
   TP        : TP3 - E25
   ---------------------------------------------------------------------------
   config.js : Définit l'URL de base pour les requêtes AJAX vers le serveur
   ============================================================================ */

/**
 * BASE_URL
 * Constante globale définissant la racine des requêtes côté client.
 * À modifier uniquement si le chemin du projet change dans l'URL.
 */
const BASE_URL = '/TP3';

// Exemple :
// fetch(BASE_URL + '/epices/lister')
//     .then(response => response.json())
//     .then(data => console.log(data));
